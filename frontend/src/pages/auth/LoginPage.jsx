import { AnimatedSignIn } from '../../components/ui/sign-in';

/* FederCare login — image/stat collage + form (see components/ui/sign-in.jsx).
 * Auth still flows through AuthContext.login with role-based routing. */
const LoginPage = () => <AnimatedSignIn />;

export default LoginPage;
